<Window x:Class="VanillaLauncher.Client.UI.SetAdminPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VanillaLauncher.Client.UI"
        mc:Ignorable="d"
        Title="Задать пароль администратора" Height="260" Width="380"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <StackPanel Margin="16">
        <TextBlock Text="Это первый запуск Admin-режима на этой машине — задай пароль."
                   TextWrapping="Wrap" Margin="0,0,0,12" />

        <TextBlock Text="Пароль:" Margin="0,0,0,4" />
        <PasswordBox x:Name="PasswordBox1" Margin="0,0,0,8" Padding="4" />

        <TextBlock Text="Повтори пароль:" Margin="0,0,0,4" />
        <PasswordBox x:Name="PasswordBox2" Margin="0,0,0,8" Padding="4" />

        <TextBlock x:Name="ErrorText" Foreground="Red" TextWrapping="Wrap"
                   Margin="0,0,0,8" Visibility="Collapsed" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Padding="12,6" Margin="0,0,8,0" IsCancel="True" Click="CancelButton_Click" />
            <Button Content="Задать пароль" Padding="12,6" IsDefault="True" Click="SetButton_Click" />
        </StackPanel>
    </StackPanel>
</Window>
